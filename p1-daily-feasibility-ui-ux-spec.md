# P1 開發建議與設計規格：日程負荷與可行性鏡頭

**版本：** 1.0（供確認）  
**作者：** Manus AI  
**日期：** 2026-08-18  
**狀態：** 僅限產品與設計規格；未授權實作、資料遷移、付費服務、外部 API 或正式網站變動。

## 1. 下一個 P1 的建議

在 P0「旅程健康與今日作戰卡」已把**現在要做甚麼**轉成可見行動後，建議下一個 P1 工作包為 **「日程負荷與可行性鏡頭」**。它回答的不是「如何自動代替使用者規劃」，而是「這一天目前是否過滿、時間是否太緊、以及哪一段最值得先檢查」。

這是目前最合適的 P1，因為它直接延伸既有的 itinerary 日期、開始時間、排序、地點、住宿與交通資訊，而不需要新帳戶權限、外部信箱、付款流程或長駐背景服務。Sygic 將一天的需求程度作為日別行程理解的一部分；Wanderlog 則把單日地點、路線、交通模式、總移動時間與可逆的路線建議放在相同工作流中。[1] [2] Trip-WebApp 應借用「讓負荷可見」的模式，而不在第一版複製其自動重排功能。

> **P1 核心承諾：** 系統會解釋一天為何顯得過緊或資料不足，並把使用者帶到可自行修正的行程位置；系統絕不自動調整時間、刪除景點或承諾真實世界交通時間。

| 候選 P1 工作包 | 價值 | 現有資料準備度 | 風險 | 建議次序 |
|---|---|---:|---:|---:|
| **日程負荷與可行性鏡頭** | 高；直接降低旅行當日的安排焦慮 | 高；已有日期、時間、排序、地點與交通段 | 低至中；需避免假精準與過度警示 | **P1.1，現在推薦** |
| 想去清單 → 日別排程 | 高；改善探索到排程的工作流 | 中；需要候選地點資料模型 | 中；涉及新增資料表、地圖／拖放流程 | P1.2 |
| 協作角色與變更摘要 | 中；降低多人溝通成本 | 中；已有成員與稽核基礎 | 中；需先確認角色模型與通知邊界 | P1.3 |

## 2. 產品目標、非目標與成功定義

這個功能服務已登入的行程成員，尤其是行程已排進多個地點、但仍未確定一天是否可行的小團體。它同時適用於出發前檢查與旅途中重新查看今天／明天；它不是導航、天氣預報、即時延誤或自動旅行社。

| 目標 | 成功表現 | 非目標 |
|---|---|---|
| 讓每一天的時間壓力可見 | 使用者可在 3 秒內讀到「寬鬆／可行／偏緊／資料待補」與主要原因 | 不產生單一 0–100 偽精準分數 |
| 讓提示可以行動 | 點擊原因可定位到該日、相鄰站點或時間表 | 不自動移動、刪除、合併或更改行程 |
| 先使用可信資料 | 只基於已有時間、項目數、相鄰時間差與已保存交通資訊 | 不在背景呼叫 Directions API 或聲稱即時交通 |
| 與 P0 健康卡互補 | 「今天安排較緊」可作為健康訊號，但完整解釋只在路線頁 | 不新增另一個常駐首頁大型卡片 |

## 3. 資料能力與計算邊界

目前 `ItineraryItem` 已提供 `Date`、`Time`、`Activity_Name`／`Activity`、`Location`、`Sort_Order`、`Lat`、`Lng`，而路線頁亦已有交通段呈現。第一版不建立新的資料表；它每次讀取行程時，以純前端函式導出每日鏡頭狀態。這使所有協作者讀到相同規則，同時避免把易過時的「健康結果」寫入資料庫。

| 資料訊號 | P1.1 可安全導出 | 使用者看到的說明 | 不可推論的內容 |
|---|---|---|---|
| 已排序活動數 | `0`、`1–4`、`5–6`、`≥7` 項 | 「今天共 7 項活動」 | 活動必然需要多少停留時間 |
| 相鄰開始時間 | 時間差 `<15`、`15–29`、`≥30` 分鐘 | 「10:30 與 10:40 只相隔 10 分鐘」 | 使用者一定遲到 |
| 無時間活動 | 項目沒有 `Time` | 「2 項尚未設定時間」 | 可放進哪一個時段 |
| 首尾行程時間窗 | 最早與最晚的已設定時間 | 「第一站 08:30；最後一站 20:15」 | 全日實際完成時間 |
| 已保存交通段 | 現有交通卡已有的分鐘／距離或模式 | 「已有 3 段交通資料」 | 即時塞車、班次、延誤或轉乘保證 |
| 地點座標 | 相鄰項目均有座標時可標示「可比較位置」 | 「有 2 段可在地圖檢查」 | 未明確呼叫路線服務時的交通時間 |

## 4. 日程負荷判定規則

P1 不用單一總分，而採取**最嚴重可解釋原因優先**。顏色與文案必須同時出現；Journey Yellow 表示「值得檢查」，深紅只保留給明確資料失敗，並不用於一般繁忙日。

| 狀態 | 預設條件 | 顯示標籤 | 說明文案範例 | 視覺材料 |
|---|---|---|---|---|
| 尚未安排 | 0 項活動 | `尚未安排` | 「可先加入第一個站點。」 | 暖白底、墨黑文字 |
| 資料待補 | 至少 2 項活動沒有時間，或沒有任何可讀時間 | `時間待補` | 「2 項活動尚未設定時間，暫時無法判斷節奏。」 | 暖白底、Journey Yellow 小點 |
| 寬鬆 | 1–4 項活動；有時間項目之間至少 45 分鐘；沒有 P1.1 警示 | `節奏寬鬆` | 「可保留彈性移動與休息時間。」 | 暖白底、墨黑文字 |
| 可行 | 5–6 項活動；最短已設定時間差 30–44 分鐘 | `節奏可行` | 「行程完整；建議出發前確認交通。」 | 墨黑邊線、暖白底 |
| 偏緊 | `≥7` 項活動，或任何相鄰已設定時間差 15–29 分鐘 | `安排偏緊` | 「最短間隔 20 分鐘，建議先檢查這一段。」 | Journey Yellow 標記＋文字理由 |
| 需要檢查 | 任一相鄰已設定時間差 `<15` 分鐘；或首尾行程跨度超過 12 小時且活動 `≥6` 項 | `需要檢查` | 「10:30 與 10:40 只相隔 10 分鐘。」 | Ink Black 標題、Journey Yellow 邊線，不以錯誤紅處理 |

若同時符合多個條件，狀態依序為：`需要檢查 → 偏緊 → 資料待補 → 可行 → 寬鬆 → 尚未安排`。用戶看到的原因最多兩條；其餘以「另有 N 項可檢查」聚合，以避免 P1 變成通知中心。

## 5. 核心使用者流程

### 5.1 出發前：發現過緊日子

使用者在「規劃路線」開啟一趟即將出發的行程。橫向日子卡片的標頭顯示 `安排偏緊`，以及簡短的「7 項活動 · 最短相隔 20 分鐘」。使用者點擊這個狀態，不會看見新的 modal，而是在原日子卡內展開一個可關閉的「日程鏡頭」區塊。該區塊指出兩個最值得檢查的相鄰站點，並提供 `查看時間表` 的唯一主要操作。使用者可自行改時、拖動、刪除或保留；系統只在資料變更後重新計算。

### 5.2 旅行中：由 P0 健康卡深入原因

P0 健康卡如發現今天偏緊，只顯示一條 `今天安排較緊` 訊號與「可先確認交通與下一站時間」。點擊後會深連結到今天的日子卡，並打開日程鏡頭。使用者始終先看「下一站」，再看「一天的節奏」，避免在首頁同時面對過多資訊。

### 5.3 資料不足：不以警告製造焦慮

如果一天有五個活動但其中三個沒有時間，系統顯示 `時間待補` 而不是把它錯判為寬鬆。點擊後只定位至沒有時間的活動並顯示「設定時間後才可比較日程間隔」。系統不為使用者猜測開始時間。

## 6. UI/UX 規格

### 6.1 行程日子卡標頭

狀態放在既有日期、住宿與活動數旁，不增加新工具列或每個活動的彩色 badge。它是按鈕；若使用者不需要詳細原因，可保持收起。

```text
┌─────────────────────────────────────────────────┐
│ 第 4 天 · 08/12                  7 項活動        │
│ 住宿：大阪難波旅館                                │
│ [ ● 安排偏緊 · 最短相隔 20 分鐘              › ] │
└─────────────────────────────────────────────────┘
```

| 元素 | 桌面 | iPhone |
|---|---|---|
| 日子狀態 | 日期標頭右側，最少 36px 高 | 日期下方全寬按鈕，最少 44px 高 |
| 橫向日子卡 | 保持現有 day-card 水平滑動 | 卡片寬度 `calc(100vw - 40px)`，下一張露出 16–24px |
| 日程鏡頭 | 在日子內容上方展開；兩列原因可並排 | 單欄排列，在活動清單之前；不遮蓋底部導航 |
| 色彩 | Ink Black 文字、Canvas Ivory 表面、Journey Yellow 注意點 | 同一系統；無藍字白底與高飽和警報色 |

### 6.2 展開的「日程鏡頭」

```text
┌─────────────────────────────────────────────────┐
│ DAY LENS / 第 4 天                    需要檢查   │
│                                                 │
│ 08:30 早餐  →  08:40 大阪城          相隔 10 分鐘 │
│ 7 項活動 · 08:30–20:15 · 3 段交通資料            │
│                                                 │
│ [ 查看時間表 ]                收起               │
└─────────────────────────────────────────────────┘
```

「查看時間表」切換到既有時間表視圖，不建立第二種日程檢視。若最短間隔原因對應兩個項目，系統短暫給這兩張卡 Ink Black outline；不使用閃爍、連續動畫或自動捲動覆蓋使用者操作。

### 6.3 P0 健康卡整合

P0 只新增／保留一條條件式訊號，例如 `● 今天安排較緊`。該訊號在概覽卡上是次要列，點擊才進入 `?tab=itinerary&focus=today&lens=load`。若今天沒有偏緊訊號，P0 健康卡不顯示任何「節奏良好」獎章，避免無關提醒。

### 6.4 微文案與互動

| 情境 | 應使用的文案 | 不應使用的文案 |
|---|---|---|
| 小間隔 | 「10:30 與 10:40 只相隔 10 分鐘」 | 「行程不可能」 |
| 項目很多 | 「今天共 8 項活動；可先檢查交通。」 | 「行程太滿」 |
| 時間缺失 | 「2 項活動尚未設定時間」 | 「資料錯誤」 |
| 寬鬆 | 「可保留彈性移動與休息時間」 | 「完美行程」 |
| 交通待核對 | 「可在時間表查看已保存交通段」 | 「預計一定準時到達」 |

## 7. 技術設計與資料模型

### 7.1 P1.1 前端導出層

新增 `src/lib/dayFeasibility.ts`，採用與 `tripHealth.ts` 相同的純函式設計。它接受 `ItineraryItem[]`、指定日子與可選的現有交通摘要，輸出下列型別：

```ts
type DayLoadLevel = 'empty' | 'needs-time' | 'relaxed' | 'balanced' | 'tight' | 'review';

interface DayFeasibilityReason {
  id: 'activity-count' | 'short-gap' | 'long-window' | 'missing-time' | 'transport-coverage';
  label: string;
  detail: string;
  itemIds?: string[];
}

interface DayFeasibility {
  date: string;
  level: DayLoadLevel;
  label: string;
  activityCount: number;
  firstTime?: string;
  lastTime?: string;
  reasons: DayFeasibilityReason[];
}
```

函式不得寫入 Supabase、不得呼叫 Google Maps，也不得依賴使用者本地瀏覽器外的不可重現資料。它以已排序的 `Time`、`Sort_Order` 及項目數進行計算，因此能獨立做 Vitest 單元測試。

### 7.2 元件與路由責任

| 元件／位置 | 責任 | 不負責 |
|---|---|---|
| `DayLoadBadge` | 日子標頭顯示狀態與一句原因 | 不計算完整規則、不寫入資料 |
| `DayLens` | 展開兩條主要原因與時間表深連結 | 不直接編輯活動 |
| `ItineraryTab` | 將導出結果與現有日子卡、時間表、活動高亮連結 | 不自動重排、不可在 render 寫 state |
| `TripHealthCard` | 在需要時顯示一條 P1 訊號與深連結 | 不複製完整鏡頭資料 |
| `dayFeasibility.ts` | 可重複、可測試的純計算 | 不接觸 DOM、網路或 Toast |

路由只增加可選查詢參數 `lens=load` 與 `focusDay=YYYY-MM-DD`。沒有這些參數時，既有行程路由與橫向日子卡行為不得改變。

### 7.3 P1.1 不需要的資料庫變更

第一版不需要 schema migration。若使用者日後明確要求各日自訂活動上限、個人步行偏好或手動忽略某一條提示，才另行設計 `trip_day_preferences`，並先確認 RLS、協作角色與同步規則。這不在目前規格內。

### 7.4 P1.2 的明確升級門檻

只有在 P1.1 驗證顯示「使用者常因交通時間調整日子」後，才考慮一個**按鈕觸發、可預覽、可復原**的交通估算／路線建議。參考 Wanderlog 的方式，任何路線優化都必須是由使用者在單日中選擇起點、終點後主動請求，並能清楚查看更動或復原。[1]

在沒有明確批准前，P1.2 不會在背景呼叫 Directions API；尤其不會把估算當作日本公共交通的承諾。Wanderlog 亦指出其日本 transit 方向資料存在限制，這正說明第一版應把保存的交通備註與人工確認放在演算法之前。[2]

## 8. 無障礙、效能與信任規則

| 範疇 | 要求 |
|---|---|
| 色彩與文字 | 每個負荷狀態均有明確文字，Journey Yellow 只作注意補充，不是唯一訊號。 |
| 鍵盤 | 日子狀態按鈕、展開鏡頭、時間表 CTA 按視覺順序 Tab；focus 沿用 Ink Black outline 和 Canvas Ivory halo。 |
| 螢幕閱讀器 | 狀態按鈕的 accessible name 包含日期、狀態與第一個原因，例如「8 月 12 日，安排偏緊，最短相隔 20 分鐘」。 |
| 動效 | 展開鏡頭只使用 180–220ms opacity／translate；尊重 `prefers-reduced-motion`；不因狀態變化閃爍。 |
| 效能 | 每次 itinerary 資料改變時以 `useMemo` 對每一天計算一次；不要在每張活動卡內重複排序完整行程。 |
| 離線 | 只使用已下載的行程資料；無法讀取交通摘要時顯示「交通資料待核對」，不把它標成同步錯誤。 |
| 協作 | 負荷是共同可見的解釋，不是對某一位編輯者的評價；不在 P1.1 增加作者追蹤或即時衝突鎖。 |

## 9. 驗收條件與產品指標

| 類別 | 驗收條件 |
|---|---|
| 計算 | `≥7` 項、短於 15 分鐘、15–29 分鐘、缺時間、空白日、同時間與跨午夜邊界均有單元測試。 |
| 可解釋性 | 每個 `tight`／`review` 狀態至少顯示一條可讀原因，且原因能定位至日子或相鄰活動。 |
| 不變性 | 開啟、展開或關閉鏡頭不可寫入 itinerary、expenses、bookings 或成員資料。 |
| 手機 | iPhone 393×852 下，日子標頭、狀態、鏡頭與底部導航不重疊；橫向日期卡仍可滑動。 |
| 路由 | P0 健康卡的深連結可聚焦指定日子並展開鏡頭；沒有參數時維持現在的行程頁。 |
| 產品指標 | 收集不含內容的事件：`day_lens_opened`、`load_reason_viewed`、`timetable_opened_from_lens`、以及健康訊號深連結率。 |

## 10. 建議分期與確認決策

### 10.1 實作分期

| 分期 | 內容 | 成功門檻 |
|---|---|---|
| P1.1a | `dayFeasibility.ts`、單元測試、日子標頭 badge、可展開 Day Lens | 系統能解釋而非只標籤任何一天的狀態 |
| P1.1b | P0 健康卡條件式深連結、指定日子／相鄰活動 focus、時間表 CTA | 健康提示可把人帶到正確調整位置 |
| P1.1c | 匿名產品事件、可選交通摘要顯示與 UX 驗證 | 能判斷提示是否真的促成使用者查看／調整 |
| P1.2（需另行批准） | 按鈕觸發的交通估算或可復原單日路線建議 | 先確認 API 成本、私隱及日本 transit 資料品質 |

### 10.2 實作前需確認的決策

| 決策 | 建議預設 | 需要確認的原因 |
|---|---|---|
| A. P1 主工作包 | 只實作 P1.1 日程負荷與可行性鏡頭。 | 避免同時啟動候選地點、協作與交通 API。 |
| B. 預設門檻 | `≥7` 項為偏緊；相鄰 `<15` 分鐘為需要檢查；`15–29` 分鐘為偏緊。 | 這些門檻會影響提醒數量與可信度。 |
| C. 行程跨度 | `≥6` 項且首尾超過 12 小時才顯示「需要檢查」。 | 避免把長但合理的旅行日誤判為問題。 |
| D. 使用者控制 | 第一版沒有手動略過／自訂門檻，只有查看原因與自行調整。 | 保持資料模型與協作規則簡單；日後可另行批准偏好設定。 |
| E. P0 整合 | 只有 `tight`／`review` 顯示健康訊號；`relaxed`／`balanced` 不顯示獎章。 | 維持 P0 的安靜且不打擾原則。 |
| F. 交通 API | P1.1 不背景呼叫 Directions API，也不自動重排。 | 維持免費層成本、資料可信度與使用者控制。 |
| G. P1.2 次序 | 日程鏡頭驗證後，才設計「想去清單 → 日別排程」。 | 先確認使用者是否需要行程可行性，避免功能表面膨脹。 |

## References

[1]: https://help.wanderlog.com/hc/en-us/articles/13545624787867-Optimize-route "Wanderlog Help Center — Optimize route"
[2]: https://wanderlog.com/blog/faq/ "Wanderlog — Frequently asked questions"
[3]: https://www.sygic.com/travel "Sygic Travel Maps"
[4]: ./trip-webapp-market-improvement-report.md "Trip-WebApp 市場基準、優勢缺口與改善路線報告"
[5]: ./trip-health-today-card-ui-ux-spec.md "Trip Health 與今日作戰卡：UI/UX 設計規格"
