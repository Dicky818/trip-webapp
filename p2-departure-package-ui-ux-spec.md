# P2 設計規格：出發前包與離線就緒中心

**產品：** Trip-WebApp  
**作者：** Manus AI  
**日期：** 2026-08-18  
**狀態：** 供批准；未授權任何程式、資料庫、權限或正式網站變動  
**前置成果：** P0「旅程健康與今日作戰卡」及 P1.1「日程負荷與可行性鏡頭」已完成。

## 1. P2 建議摘要

建議 P2 優先實作 **「出發前包與離線就緒中心」**（Departure Package & Offline Readiness），而不是立即加入系統推播、背景定位或完整離線地圖。這是一個由 Trip Portal 工具卡開啟的單一工作區，讓旅伴在出發前能回答三件事：**我可離線查看甚麼、哪些重要資料仍不完整、以及在無網絡時應如何取回可信的行程摘要。**

市場資料支持這個方向。TripIt 說明已登入的使用者可離線查看旅遊資訊，但離線資料為靜態內容，連線恢復後才更新。[1] Wanderlog 則以已下載標記表明行程、備註、地點與附件可供離線取用。[2] Sygic Travel Maps 亦把日別行程、地圖與離線可用性視作同一段出發前體驗。[3] Trip-WebApp 已有 PWA、Service Worker、中文 PDF、行程、航班、住宿、預訂及離線支出佇列；P2 的價值在於使這些能力可被理解與檢查，而不是假裝所有內容均可離線。

> **P2 定位：** 一個誠實、可攜帶、可核對的「出發前包」，而不是另一個資料輸入頁或自動導航工具。

| 候選範圍 | 使用者價值 | 與現有能力的相容度 | P2 優先度 | 決定 |
|---|---|---:|---:|---|
| 出發前包與離線就緒中心 | 高：降低出發時資料分散與網絡焦慮 | 高：可重用 PDF、PWA、行程、住宿及航班資料 | 最高 | **本規格範圍** |
| 情境化 AI 建議 | 中高：改善空檔或過緊日的選擇 | 中：需要更嚴格的資料依據與成本保護 | 次序二 | 後續 P2.2 |
| App 內提醒 | 中：支援出發當日 | 中低：要先驗證訊號價值與通知權限 | 次序三 | 後續評估 |
| 全離線地圖／導航 | 中：偏遠地區有價值 | 低：涉及地圖資料、儲存與供應商邊界 | 不建議此輪 | 明確排除 |

## 2. 產品目標與非目標

P2 的目標是在一個可由手機單手操作的頁面中，生成出發所需的可信摘要、離線狀態和缺口清單。它應讓使用者在起飛前完成一次準備，在旅途中用最少網絡取回時間、地址、航班、住宿、確認資料和同伴資料。

| 目標 | 成功表現 |
|---|---|
| 清楚呈現可離線內容 | 使用者可分辨「已保存至此裝置」、「需要網絡」與「資料尚未完整」。 |
| 把分散資料變成出發檢查 | 航班、住宿、今天／明天行程、地址和 PDF 在同一個低干擾入口。 |
| 誠實描述限制 | 不聲稱地圖、AI、搜索、航班即時狀態或未下載附件可離線使用。 |
| 不擴大敏感資料 | 不收集護照、信用卡、支付卡、醫療、私人住址以外的敏感內容；不自動匯入郵件。 |

P2 **不包括**通知推播、背景定位、離線地圖瓦片、離線 Directions、Gmail 權限、掃描新的護照或登機證、支付功能、共享裝置資料、或自動變更任何行程。這些項目需要不同的安全、隱私或供應商評估，故不應混入首版。

## 3. 使用者與核心情境

主要使用者是已加入行程、正準備出發的小型團體成員。擁有者與協作者均可建立自己的本機出發前包；因為離線資料是裝置層的副本，而不是新的共享真相來源，任何成員的保存操作不會改變其他成員的行程資料。

| 情境 | 使用者任務 | 系統回應 | 不應發生的行為 |
|---|---|---|---|
| 出發前一天 | 「我的重要資料是否已準備？」 | 顯示完整度、缺口、最新同步時間與一個主要「更新出發前包」操作。 | 不自動下載大型資料或修改旅程。 |
| 機場／飛機上 | 「我要看航班、住宿地址和第一天路線。」 | 顯示靜態快照、地址、時間、確認資料及已下載 PDF。 | 不顯示過期資料為即時資訊。 |
| 無網絡下記錄支出 | 「這筆支出有沒有安全儲存？」 | 指向現有離線佇列狀態，顯示待同步筆數。 | 不假裝已同步到雲端。 |
| 連線恢復 | 「資料是否是最新？」 | 顯示上次包更新時間及「重新整理」建議。 | 不在背景覆寫正在編輯中的行程。 |

## 4. 資訊架構與入口

P2 由 Trip Portal 的 `06 / GUIDE` 或獨立的 `07 / READY` 工具卡進入。為免原有六卡格局失衡，建議將原本的 `TRAVEL SUPPORT` 卡改名為 **「READY / 出發前包」**，而「旅程助手」保留在概覽健康卡的次要連結和現有底部導航。這個改名只改資訊層級，不刪除 AI 功能。

桌面版在 Trip Pass 下方顯示一行 `READY / DEPARTURE PACKAGE` 健康狀態；手機版則將其放在 Trip Pass 後、工具卡之前。當旅程開始前 72 小時內、或離線佇列有待同步支出時，該入口改以 Journey Yellow 邊框提醒，但不使用紅色警報。

| 層級 | 元素 | 內容與作用 |
|---|---|---|
| L1 | 出發前包總覽 | 標題、最新準備時間、整體狀態、唯一主要 CTA。 |
| L2 | 就緒檢查 | 行程、航班、住宿、PDF、同步的五個可展開列。 |
| L2 | 離線快覽 | 下一個航班／住宿／今天與明天首項行程；僅在資料存在時顯示。 |
| L3 | 靜態詳情 | 地址、時間、航班號、確認資料、旅伴名稱與可下載 PDF。 |
| L3 | 限制說明 | 「需要網絡」內容、同步待辦和資料新鮮度文字。 |

## 5. 桌面與手機版面規格

視覺系統沿用 Trip Portal：Canvas Ivory `#F5F2E8` 背景、Paper White 內容卡、Ink Black 主狀態、Journey Yellow 主要 CTA。任何離線或缺口狀態使用文字、圖示和邊框組合，不能只依賴顏色。

| 區域 | 桌面（≥1024px） | 手機（≤767px） |
|---|---|---|
| 頂部摘要 | 左方狀態與更新時間；右方 Journey Yellow「更新出發前包」。 | 全寬墨黑摘要卡；主要 CTA 置底，最少 48px 高。 |
| 就緒檢查 | 兩欄卡片，固定順序：行程、航班、住宿、PDF、同步。 | 單欄五列；只顯示狀態與一句原因，點擊展開。 |
| 離線快覽 | 右側窄欄，最多顯示三項即將發生資料。 | 置於檢查列後，以水平滑動的三張摘要卡呈現。 |
| 缺口行動 | Contextual text link，例如「新增住宿地址」。 | 每列只顯示一個文字操作；不放置多個圖示按鈕。 |
| 底部安全區 | 沿用既有側欄與內容區。 | 保留 `safe-area-inset-bottom`，內容底部至少預留 96px，避開五項底部導航。 |

## 6. 就緒狀態模型與文案

首版採用可解釋的四段狀態。它不計算虛假的百分比，而是顯示五個資料面向中已有多少項可供閱讀。只有當資料確實成功快照到本機時，才可標示「已保存」。

| 狀態 | 定義 | 視覺 | 主要文案 | 主要行動 |
|---|---|---|---|---|
| `ready` | 所有有資料的面向已保存，無待同步支出。 | Ink Black 印章＋Journey Yellow 點綴。 | 「出發前包已準備」 | 「查看離線快覽」 |
| `review` | 行程可用，但存在缺少地址、確認資料或尚未生成 PDF。 | 暖白底＋黃邊。 | 「尚有 2 項需要確認」 | 「查看準備清單」 |
| `pending_sync` | 有待同步支出或目前離線。 | 中性灰底＋同步圖示。 | 「離線資料安全儲存中」 | 「立即同步」或「了解狀態」 |
| `stale` | 包超過 24 小時未更新，或旅程資料在後來被修改。 | 灰色時間戳＋金色點。 | 「上次準備：昨天 19:20」 | 「更新出發前包」 |

「行程」就緒需要至少有已保存的旅程名稱、日期、每日行程及住宿對應；「航班」與「住宿」在該類別有資料時才參與完整度；「PDF」表示旅行手冊已成功生成並在目前裝置取得；「同步」僅反映現有離線支出隊列。沒有航班資料時，文案應寫「未加入航班資料」，不能寫「航班未準備」。

## 7. 核心互動流程

### 7.1 更新出發前包

使用者點擊唯一的主要 CTA 後，系統先計算資料範圍並展示確認小表單，而不是立即進行大型下載。表單必須列出：行程摘要、航班、住宿、預訂、今日與明天行程、旅伴名單、PDF，以及「本機離線支出狀態」。使用者可取消，或選擇「保存至此裝置」。

保存過程採 3 步進度：`1 / 讀取現有旅程資料`、`2 / 準備離線摘要`、`3 / 完成並記錄時間`。任何不能保存的項目會被列為「需要網絡」，不會令整個流程失敗。完成後顯示「已準備 8 項資料；地圖與 AI 仍需要網絡」，並提供「查看離線快覽」。

### 7.2 無網絡下的使用

若使用者離線打開出發前包，系統只讀取最後一次成功快照；頂部以固定文字標示「離線快照，最後更新於 …」。這符合 TripIt 對離線資料為靜態、連線後才更新的明確描述。[1] 按下需要網絡的控制時，顯示非阻塞提示，例如「地圖路線需要網絡；你仍可查看地址和日程」。

### 7.3 缺口修正

每個就緒列最多一個修正操作。例：住宿缺地址時連往現有住宿表單；PDF 尚未生成時連往既有「匯出小冊子」；待同步支出則重用現有 `立即同步`。任何修正後只重新評估就緒狀態；不會自動生成或覆寫資料。

## 8. 資料、快取與技術設計

首版應使用既有讀取 API：`Trip`、`ItineraryItem`、`Flight`、`Accommodation`、`Booking`、`TripMember`、現有 PDF 匯出與 `useOfflineSync`。新的純函式 `deriveDepartureReadiness()` 會把只讀資料轉換為狀態、缺口、可保存區段和顯示文案；此函式必須有 unit tests，且不可直接寫入 Supabase。

本機快照建議使用現有 IndexedDB 基礎（已用於離線支出）新增具版本的 `departure-package:{tripId}:{userId}` key。內容為最小化 JSON 摘要，不保存原始收據影像、密碼雜湊、付款卡、護照或未使用附件。快照應記錄 `preparedAt`、`sourceUpdatedAt`、`schemaVersion`、`contentSections` 與 `warningCodes`。

| 層次 | 元件／責任 | 寫入邊界 |
|---|---|---|
| `lib/departurePackage.ts` | 衍生就緒狀態、建立最小快照、判斷新鮮度。 | 不寫 Supabase。 |
| `hooks/useDeparturePackage.ts` | 讀取／更新本機 IndexedDB、處理離線回退。 | 只寫本機裝置。 |
| `DeparturePackageCard` | Trip Portal 摘要與入口。 | 無資料庫寫入。 |
| `DepartureChecklist` | 五列就緒狀態與單一修正連結。 | 調用既有表單或同步流程。 |
| `OfflineBrief` | 顯示已保存快照。 | 純閱讀。 |

不需要新增 Supabase 資料表、Edge Function、檔案上傳或新權限。由於快照屬於個人裝置，其他協作者不會自動取得；他們需自行在各自裝置準備。這比建立跨裝置敏感快取更符合最小資料原則。

## 9. 無障礙、隱私與性能要求

所有狀態徽章須有完整文字；`ready`、`review`、`pending_sync` 與 `stale` 不可只用顏色區分。展開列採原生可鍵盤操作的按鈕及 `aria-expanded`，焦點沿用 Ink Black／Canvas Ivory ring。倒數或更新時間不使用不停變動的動畫；同步進度使用 `role="status"` 與簡短、禮貌的 live region。

快照建立時不得阻塞行程編輯，且應先建立小型文字資料，再處理可選 PDF。首版不預載 Google 地圖瓦片、不保存原始附件、不串流大型檔案；此設計可避免 PWA 快取膨脹及免費層儲存風險。若快照大小超過 1 MB，系統只保存最後兩天、即將到來的航班與住宿摘要，並告知使用者。

## 10. 分期開發建議與驗收

| 階段 | 內容 | 依賴 | 驗收標準 |
|---|---|---|---|
| P2.1 | 就緒判斷、Trip Portal 入口、五列檢查與本機摘要。 | 既有 API、IndexedDB、P0 同步狀態。 | 離線重開時能顯示最後快照及明確時間。 |
| P2.2 | PDF 快覽、缺口修正深連結與現在／明天行程摘要。 | 現有 PDF、行程、住宿與航班表單。 | 每個缺口最多一個修正操作，且不會自動寫入。 |
| P2.3 | 使用量驗證後再評估情境化 AI 或 App 內提醒。 | P2.1 事件與使用者回饋。 | 只有資料依據明確的建議可被展示；不直接改行程。 |

首版應測試：行程有／無航班、有／無住宿、空日程、離線待同步支出、失敗的 PDF 生成、超過 24 小時快照、兩位協作者在不同裝置各自準備，以及無網絡重開後的靜態內容。成功指標包括完成更新出發前包的比例、離線快覽打開率、缺口修正完成率、及使用者在離線狀態下仍能找到下一個關鍵地址／時間的比例。

## 11. 需要批准的決策

| 編號 | 決策 | 建議預設 | 原因 |
|---|---|---|---|
| A | P2 主範圍 | 出發前包與離線就緒中心。 | 直接利用現有 PWA、PDF、旅程與同步能力。 |
| B | 快照儲存位置 | 僅儲存在目前使用者的 IndexedDB。 | 不建立新敏感資料表，且各成員可獨立準備。 |
| C | 就緒列數量 | 行程、航班、住宿、PDF、同步共五列。 | 可掃讀但仍足夠完整。 |
| D | 新鮮度規則 | 超過 24 小時或資料來源較新時標示 `stale`。 | 明確傳達離線資料是靜態快照。 |
| E | 資料範圍 | 不保存原始收據／附件、護照、付款或醫療資料。 | 降低本機敏感資料與儲存風險。 |
| F | 地圖與 AI | 在包內明確列為需要網絡。 | 避免對離線能力作錯誤承諾。 |
| G | 推播與提醒 | 不納入 P2.1；先用 App 內狀態。 | 先驗證行為需求，避免通知權限與背景作業。 |
| H | 後續順序 | P2.1 → P2.2 → 使用數據後才考慮 AI／提醒。 | 控制範圍並優先建立可信資料包。 |

## 參考資料

[1] [TripIt Help Center — Using mobile apps without connectivity](https://help.tripit.com/en/support/solutions/articles/103000063436-using-mobile-apps-without-connectivity)  
[2] [Wanderlog Help Center — Download trip plan for offline access](https://help.wanderlog.com/hc/en-us/articles/13545182856859-Download-trip-plan-for-offline-access)  
[3] [Sygic Travel Maps — Official product page](https://www.sygic.com/travel)  
[4] [Trip-WebApp Market Improvement Report](./trip-webapp-market-improvement-report.md)  
[5] [P1.1 Daily Feasibility Design Specification](./p1-daily-feasibility-ui-ux-spec.md)
