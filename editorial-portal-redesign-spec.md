# Trip-WebApp「編輯式旅程入口」重設計規格

> **狀態：等待確認。** 本文件只定義建議的設計與互動方向；目前沒有修改任何 React、CSS、資料庫、Edge Function 或正式網站資產。

## 1. 設計目的

本次重設計將現有「旅途作戰桌」的可靠任務導向，轉化為參考畫面所呈現的**編輯式旅程入口（Editorial Travel Portal）**。它應像一張高質感的旅程首頁：先讓使用者在三秒內知道「我在哪一趟旅程、今天處於甚麼狀態、下一步是甚麼」，再用有順序的功能卡帶領使用者前往行程、支出、打包與協作工具。

這並不是住宿資訊頁的複製。Trip-WebApp 仍然是一個共同規劃、記帳、分帳與旅途中使用的工作工具；參考畫面提供的是**視覺語言、資訊節奏與卡片層級**，而不是其內容模型。

| 設計目標 | 成功定義 | 不做的事 |
|---|---|---|
| 先看懂旅程 | 使用者一開頁便看見目的地、日期、目前旅程日、旅伴與今天下一步。 | 不把每個資料庫欄位都放進首屏。 |
| 更少控制項 | 每個畫面一個主要 CTA；匯出、設定、分類等低頻操作收在情境選單。 | 不移除既有功能或把常用操作藏到難找。 |
| 更有旅程感 | 使用暖米白、墨黑、旅程黃與編輯式標籤建立識別。 | 不變成純裝飾的旅遊雜誌，犧牲可掃讀性。 |
| 手機優先 | iPhone 首屏、底部安全區、單手觸及與短文字優先。 | 不以桌面縮小版作為手機版。 |

## 2. 設計命題

### 2.1 核心概念：一張「旅程通行證」加一組「下一步卡」

首頁及行程概覽由兩個層次組成。第一層是深墨黑的**旅程通行證**，集中展示此行程的身份與關鍵狀態。第二層是有編號的**下一步卡**，把高頻工具整理成少量可辨識的入口。這種結構讓資訊由「我是誰／在哪裡」自然過渡到「現在可以做甚麼」。

### 2.2 設計原則

| 原則 | 實作準則 |
|---|---|
| **一屏一主角** | 首屏只有一張主旅程卡；下方功能卡不與主卡爭奪注意力。 |
| **編號即脈絡** | 卡片的 `01 / PLAN`、`02 / SPEND` 等小標只用作掃讀錨點，不取代清楚的中文標題。 |
| **黑白為骨、黃色為訊號** | 墨黑處理旅程身份與已選取狀態；近白處理閱讀面；黃色只表示今日、完成、主要行動或需要立即注意。 |
| **內容直排、日期橫移** | 每日行程與支出日期可以橫向切換；一個日子／日期卡內的內容必須保持垂直，方便閱讀長清單。 |
| **按鈕是承諾** | 每一個明顯按鈕都要對應明確動作；不以多個相同權重的按鈕製造選擇壓力。 |

## 3. 視覺系統

### 3.1 色彩角色

| Token 名稱 | 色值 | 使用位置 | 用法限制 |
|---|---:|---|---|
| `Canvas Ivory` | `#F5F2E8` | 全域背景、卡片群之間的留白。 | 不用純白作大面積頁面背景。 |
| `Ink Black` | `#111111` | 主旅程通行證、已選取功能卡、重要標題。 | 深色大區塊只放高對比淺色文字。 |
| `Paper White` | `#FFFFFF` | 次級功能卡、表單、清單、對話框。 | 卡片之間使用低對比陰影，不堆疊重邊框。 |
| `Journey Yellow` | `#FFC91A` | 主 CTA、今日標記、完成標記、關鍵 badge。 | 每一屏最多一個主要黃色 CTA。 |
| `Route Blue` | `#2563EB` | 地圖、可點連結、同步／資訊狀態。 | 不再與主 CTA 競爭；保留其既有資料語意。 |
| `Slate Text` | `#697386` | 說明、日期、次要 metadata。 | 最小文字仍需符合可讀性，不用淡灰呈現關鍵狀態。 |
| `Safe Green` | `#0F8A5F` | 已同步、已完成、旅行中。 | 必須搭配圖示及文字。 |
| `Alert Red` | `#C2362B` | 破壞性動作、同步失敗、錯誤。 | 不用作一般提醒。 |

> **待確認重點：** 旅程黃會取代 Route Blue 成為主要 CTA 色；Route Blue 仍然保留給地圖、連結與資訊性狀態。這是本方向與目前「Route Blue 為主行動色」最大的視覺改變。

### 3.2 字體與數字

中文主體維持 **Noto Sans TC**，英文、日期和數字維持 **DM Sans**，以免不必要地增加字型載入。為呼應參考畫面的編號與微型標籤，可在 `01 / PLAN`、`DAY 08`、`CHECK-IN` 等位置使用 DM Sans 的大寫、600 weight、`letter-spacing: 0.16em`；不必引入新的等寬字型。

| 層級 | 字型與規格 | 例子 |
|---|---|---|
| Hero 行程名 | Noto Sans TC 30–34px / 800 | `2026 8月 大阪京都` |
| 首屏數字 | DM Sans 20–24px / 700 / tabular figures | `08 / 12`、`HKD 35,064.87` |
| 功能卡標題 | Noto Sans TC 20–22px / 800 | `每日行程`、`記錄支出` |
| 卡片編號 | DM Sans 11–12px / 700 / tracking 0.16em | `01 / PLAN` |
| 支援文字 | Noto Sans TC 13–14px / 500 / 行高 1.55 | `先看今天的下一站與交通安排。` |
| 按鈕 | Noto Sans TC 15–16px / 700 | `📍 加一站` |

### 3.3 空間、圓角與材質

頁面採用 8pt spacing system。手機外邊距為 20px；主旅程卡與功能網格間距為 24px；卡片內距為 20–24px。主旅程通行證採 28px 圓角，功能卡採 20px 圓角，輸入欄與低層小工具採 14px 圓角。陰影應是柔和的深灰擴散，例如 `0 18px 38px rgba(17,17,17,.10)`，而非多重藍色 glow。

## 4. 資訊架構與功能排序

### 4.1 手機版首頁／行程概覽

首次登入後，首頁不再以多個同權重數字卡開場，而以「目前行程」作為主要內容。使用者如有多個行程，只顯示一張目前／最近行程通行證，其他行程以簡潔的橫向切換或「查看全部行程」入口處理。

```
品牌列：路線標誌／旅途作戰桌                 帳戶與語言選單

主旅程通行證（墨黑）
  • TRIP / IN PROGRESS                              第 8 / 12 天
  2026 8月 大阪京都
  8/9 — 8/20 · HKD · 5 位旅伴
  ─────────────────────────────────────────────
  TODAY / 08.16          NEXT STOP                 SPEND SO FAR
  3 項計劃               嵐山                       HKD 35,064.87
  [ 📍 查看今天行程 ]

SECTION / YOUR TRIP TOOLS                           05
  01 / PLAN       02 / SPEND
  🗺️ 每日行程     💳 記錄支出
  下一站與交通      拍照收據、分帳與結算

  03 / PACK       04 / STAY & FLIGHT
  🎒 打包清單      ✈️ 航班與住宿
  出發前準備        確認預訂與入住資料

  05 / ASSIST
  ✨ 旅程助手
  根據目前行程整理提醒

底部主導航：概覽 / 路線 / 支出 / 打包 / 助手
```

### 4.2 功能卡對應

| 編號 | 卡片 | 中文標題 | 支援文字 | 主要目的 | 預設目的地 |
|---:|---|---|---|---|---|
| 01 | `PLAN` | 🗺️ 每日行程 | 下一站、地圖與交通安排 | 規劃與查看當天行程 | 路線／每日行程 |
| 02 | `SPEND` | 💳 記錄支出 | 拍照收據、付款與分帳 | 高頻記帳與結算 | 支出列表 |
| 03 | `PACK` | 🎒 打包清單 | 出發前的必要準備 | 任務完成感 | 打包清單 |
| 04 | `STAY & FLIGHT` | ✈️ 航班與住宿 | 預訂、入住與交通資訊 | 旅途中快速查閱 | 概覽內的預訂區／詳情抽屜 |
| 05 | `ASSIST` | ✨ 旅程助手 | 根據資料整理提醒 | AI 建議與待處理事項 | 旅程助手 |

如果「航班與住宿」在資料不足時沒有價值，卡片應以 `預訂資料尚未加入` 的溫和空狀態顯示，並提供一個次級文字連結「加入第一筆預訂」，不應顯示偽造資訊。

### 4.3 底部導航

底部五項導航保留，因為它比入口卡更適合旅途中快速切換。其角色由「功能的唯一入口」改為「任何頁面的常駐捷徑」。選中項可採淺黃色或墨黑文字，但不應同時使用滿版黃色、藍色外框與浮動陰影三種效果。

| 項目 | Emoji | 是否保留 | 原因 |
|---|---|---|---|
| 概覽 | 🧭 | 保留 | 回到旅程通行證與今天下一步。 |
| 路線 | 📍 | 保留 | 高頻查閱日程、地圖與時間表。 |
| 支出 | 💳 | 保留 | 高頻記帳與查看付款狀態。 |
| 打包 | 🎒 | 保留 | 出發前的可完成任務。 |
| 助手 | ✨ | 保留 | AI 建議與待處理事項。 |

## 5. 元件規格

### 5.1 主旅程通行證

主卡只可出現在首頁和概覽第一屏。它不應在每個子頁面重覆佔據大面積；進入路線、支出或打包後，改用高度 68–80px 的緊湊旅程列，顯示目的地、目前日與一個返回概覽的入口。

| 區塊 | 內容 | 規格 |
|---|---|---|
| Eyebrow | `• / TRIP` 或 `• / IN PROGRESS` | 12px、淺灰、全大寫；不使用多個 badge。 |
| 標題 | 行程名稱 | 最大兩行；超出時截斷而不是縮小至難讀。 |
| 狀態列 | 日期、貨幣、旅伴數 | 使用短分隔點；不再放四張獨立數字卡。 |
| 三欄摘要 | 今天、下一站、累計支出 | 平均三欄；手機太窄時可改成兩欄加一條次要文字。 |
| CTA | `📍 查看今天行程` | 全卡唯一主要按鈕，Journey Yellow 實心底。 |

### 5.2 功能卡

功能卡是摘要入口，不是所有功能的索引。卡片 2 欄排列，最少高度 156px，點擊面積覆蓋整張卡。選中的／最合理的下一步卡可使用墨黑底、白字及 Journey Yellow 圓點；其餘使用 Paper White 底與低對比邊框。

卡片在 hover 或觸控按壓時只做 `translateY(-2px)` 和陰影加深；鍵盤 focus 使用 Route Blue ring，因為黃色 ring 在米白底上對比不足。卡片不得因為純裝飾而彈跳或反覆動畫。

### 5.3 每日行程與支出

你已確認的「日子／日期橫移、內容直排」將是新系統的固定規則。

| 介面 | 橫向容器 | 卡片內部 | 滑動說明 |
|---|---|---|---|
| 每日行程 | 一天一張卡；顯示下一天的一小段邊緣。 | 住宿、景點、交通、備註由上而下排列。 | `← 左右滑動切換日期 →` 只在多於一天時顯示。 |
| 支出 | 一個日期一張卡；按日期排序。 | 該日支出由上而下排列，保留付款人、分帳與更多選單。 | `← 左右滑動切換日期 →` 只在多於一日支出時顯示。 |
| 打包 | 不需要橫向日期卡。 | 依分類直向 checklist。 | 不加滑動提示，避免模式泛濫。 |

### 5.4 表單與新增操作

`📍 加一站` 和 `💳 新增支出` 繼續是對應頁面唯一顯眼的主要動作。它們使用 Journey Yellow（而非原 Route Blue），文字必須為黑色以確保閱讀。`更多`、分類管理、CSV、匯出、複製日程與編輯行程都維持在情境選單。

新增支出繼續採三步式：**內容 → 金額與付款人 → 可選細節**。分帳預設仍是「目前登入使用者本人」，並以 `預設：你本人` 清楚說明；只有使用者主動選擇其他旅伴時才加入分帳。

## 6. 互動、動態與回饋

### 6.1 狀態語言

| 狀態 | 文字 | 色彩 | 呈現位置 |
|---|---|---|---|
| 旅行中 | `旅行中 · 第 8 / 12 天` | Safe Green | 主卡 eyebrow 或小型 pill。 |
| 下一步 | `下一站：嵐山` | Journey Yellow | 主卡 CTA 前或日程卡頂部。 |
| 待確認 | `AI 建議，請確認` | Journey Yellow 加文字 | 收據／AI 輸入欄旁。 |
| 已同步 | `已同步` | Safe Green | 操作附近的小字，不作全螢幕 toast。 |
| 離線待同步 | `已儲存在此裝置` | Journey Yellow 加圖示 | 頁面頂部或相關清單附近。 |
| 錯誤 | `尚未同步，請重試` | Alert Red 加文字 | 原操作附近，提供重試。 |

### 6.2 動畫規則

所有常規回饋遵循 140–220ms、只使用 opacity 和 transform。功能卡按壓使用 `scale(0.98)`；底部抽屜以向上位移 16px 進場；日期卡橫移依瀏覽器原生捲動和 snap，不增加視差。成功新增支出可維持現有的列表定位、淡黃色／淡藍色高亮與 7 秒復原，但其新列應落在該「日期卡」內。

在 `prefers-reduced-motion` 下，取消卡片浮升、清單 highlight 淡入和抽屜滑動；內容狀態仍必須即時可見。

## 7. 響應式與 iOS Safari 規格

手機布局以 393×852 CSS pixels 為基準，最小支援寬度 320px。主內容左右內距為 20px；日期／日子橫向卡寬為 `min(88vw, 34rem)`，讓下一張卡露出至少 16px。底部導航與 PWA 安裝提示均需使用 `env(safe-area-inset-bottom)`，兩者不可重疊。

| 尺寸 | 首頁工具卡 | 主旅程通行證 | 導航 |
|---|---|---|---|
| 320–639px | 2 欄；最少 148px 高 | 28px 圓角；三欄摘要可壓成 2+1 層級 | 固定底部五項。 |
| 640–1023px | 2–3 欄；保留閱讀留白 | 可加入更多狀態，但不可塞入完整預訂清單 | 底部導航或側欄依裝置方向決定。 |
| 1024px 以上 | 3 欄工具卡，維持任務軌道側欄 | 主卡仍限制內容寬度，不拉成全頁橫幅 | 左側任務軌道加內容工作區。 |

## 8. 無障礙與內容規則

Emoji 必須與文字共存，例如 `📍 每日行程`，不可只使用 `📍` 作按鈕標籤。每張可點擊卡必須具備鍵盤 focus、語義化按鈕或連結角色和可理解的 accessible name。深色主卡內的輔助文字不可低於可讀對比；黃色 CTA 使用黑字，避免白字配黃底。

水平滑動列必須保留指標／觸控拖曳、滑鼠滾輪／Shift+滾輪與鍵盤可聚焦卡片。滑動提示是輔助，不能是唯一提示；下一張卡的可見邊緣同樣要表達內容可延續。卡片內不得嵌套另一條相同方向的水平清單，避免觸控手勢衝突。

微文案保持情境在前、動作在後，例如：

> `第 8 天正在進行，先看下一站。`

> `今天有 3 項安排，向右切換下一天。`

> `你先負責這筆支出；需要時再加入旅伴分帳。`

## 9. 實作範圍與不變項目

如獲批准，實作將優先重設 **首頁、行程概覽、桌面側欄、手機底部導航的選中狀態、功能卡，以及主旅程通行證**。路線、支出、打包和助手的資料模型、Supabase RLS、分享密碼、離線佇列、收據辨識、Google Maps、PDF、AI Function 與 PWA 邏輯均保持不變。

| 類別 | 會改動 | 不會改動 |
|---|---|---|
| 視覺 | 色彩角色、字級、卡片、陰影、留白、圖示與狀態排列。 | 行程、支出、預訂的資料內容。 |
| 導航 | 首頁入口、工具排序、次要操作收納、底部選中樣式。 | 五項主導航的核心目的地。 |
| 互動 | CTA 層級、卡片點擊、更多選單、日期／日子卡。 | 現有離線同步、Undo 與分享安全流程。 |
| 技術 | React/Tailwind 元件樣式與少量布局結構。 | 資料庫 schema、Supabase 權限、Edge Functions。 |

## 10. 需要你確認的決策

請逐項確認或提出修改。收到確認前，我不會實作。

| 編號 | 決策 | 建議 | 請確認 |
|---:|---|---|---|
| A | 主色策略 | Journey Yellow `#FFC91A` 成為唯一主要 CTA；Route Blue 改為地圖／資訊色。 | 同意／修改 |
| B | 主視覺 | 首頁與概覽使用墨黑「旅程通行證」作首屏主卡。 | 同意／修改 |
| C | 首頁結構 | 主旅程通行證 + 5 張有編號的工具卡；保留底部五項導航。 | 同意／修改 |
| D | 語言操作 | 不採參考圖的五個語言 pills，改保留現有帳戶／語言選單，避免頂部過密。 | 同意／修改 |
| E | 字體 | 繼續使用 Noto Sans TC + DM Sans，只以字距與大寫製作編輯式小標。 | 同意／修改 |
| F | 卡片排序 | `每日行程 → 記錄支出 → 打包 → 航班與住宿 → 旅程助手`。 | 同意／修改 |
| G | 日子／日期行為 | 日子與支出日期橫向切換；卡片內內容直向排列。 | 已確認／修改 |

## 11. 驗收標準

在開始實作前，設計驗收以以下標準為準。手機首屏必須同時看見品牌、目前旅程與一個唯一主要 CTA；功能卡不超過五張，且每張皆有明確目的地；任意功能頁同時不可出現兩個以上等權重主按鈕；低頻工具必須在「更多」中仍可透過鍵盤與觸控操作；iPhone 393×852 預覽中不得出現橫向頁面溢出、底部安全區遮擋或 44px 以下的主要觸控目標。

---

**下一步：** 請回覆決策 A–G 的確認結果。收到你的批准後，我會先把本規格的最終選項寫入 `ideas.md`，再開始實作；在此之前，正式網站維持目前版本。
