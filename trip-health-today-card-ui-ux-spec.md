# Trip Health 與今日作戰卡：UI/UX 設計規格

**版本：** 1.0（供確認）  
**作者：** Manus AI  
**日期：** 2026-08-18  
**狀態：** 僅設計規格；未授權實作、資料遷移、通知、第三方服務或正式網站變動。

## 1. 目的與產品假設

Trip-WebApp 已能儲存大量旅程資料，但旅途中最重要的問題並非「資料在哪裡」，而是「**我現在應做甚麼、下一步是否安全、是否有任何需要處理的例外**」。本規格將首頁 Trip Pass 與行程概覽的現有狀態濃縮成一張可閱讀、可行動、但不打擾的 **Trip Health 與今日作戰卡**。

市場研究顯示，TripIt 的主要價值在將確認資料與提醒集中為旅途中可採取的行動，而 Wanderlog 與 Sygic 的行程／地圖模式使日別計劃與位置資訊可互相理解。[1] [2] Trip-WebApp 的設計不會模仿其推播、全信箱存取或背景定位；P0 僅使用本 App 已有的旅程、行程、航班／住宿、支出及離線同步資料。[3]

> **設計原則：** 每次開啟旅程，只回答一個主要問題、最多顯示三項需要注意的情況，並只提供一個主要行動。

## 2. 名稱、位置與範圍

### 2.1 使用者可見名稱

本功能於首頁顯示為 **「今日作戰」**，於行程概覽顯示為 **「旅程健康」**。兩者讀取相同狀態資料，但因使用情境不同而採用不同密度：首頁是快速進入點，概覽則是可展開的健康摘要。

| 位置 | 元件名稱 | 目的 | 顯示密度 |
|---|---|---|---|
| Trip Portal 首頁 | 今日作戰 | 在六張工具卡之前或其首卡位置，提供現在最重要的路線／預備行動。 | 緊湊：1 個主狀態、1 個下一站、最多 2 個健康 badge。 |
| 行程工作區 → 概覽 | 旅程健康 | 取代目前單純的 `JOURNEY / NOW` 卡，顯示完整的今日狀態、健康訊號和下一步。 | 完整：1 個主狀態、最多 3 個訊號、1 個主要 CTA。 |
| 行程工作區 → 其他頁籤 | 簡短健康標記 | 只在存在「需要注意」訊號時出現在工作區標頭；不重複完整卡片。 | 只顯示數量，例如「2 項待處理」。 |

此功能不新增底部導航，不新增第七張工具卡，也不建立新資料表。它是現有 Trip Pass、日別行程、預訂、支出及離線同步狀態的讀取層。

## 3. 資訊模型與資料來源

### 3.1 現有資料來源

| 健康訊號 | 使用現有來源 | 可顯示的內容 | P0 不做的事 |
|---|---|---|---|
| 旅程階段 | `Trip.Start_Date`、`Trip.End_Date` | 計劃中、旅行中第 N／M 天、已完成。 | 不處理跨時區自動換日。 |
| 今日／下一站 | itinerary 的日期、開始／結束時間、活動名稱、地點、交通段。 | 正在進行、下一站、今日沒有安排、下一項在明天。 | 不自動新增、移動或刪除行程。 |
| 航班與住宿 | 已標記的 flight／accommodation／booking 支出欄位。 | 已確認、待確認、已取消、入住／退房日期。 | 不偵測航空公司真實延誤，不讀取外部確認信。 |
| 同步狀態 | `useOfflineSync` 的 `online`、`pendingCount`、`isSyncing`。 | 離線已保存、等待同步、同步中、已同步。 | 不宣稱地圖、AI、搜尋或所有行程資料可離線。 |
| 支出健康 | 支出日期、基礎貨幣、分類與總額。 | 今天已記錄支出、今日仍未記帳的中性提示（只在使用者有支出習慣時才考慮 P1）。 | P0 不產生超支判斷或結算建議。 |

### 3.2 資料缺漏的處理

資料不存在不是錯誤。每個訊號必須明確標示是「尚未加入」而不是「失敗」。例如沒有航班資料時顯示「尚未加入航班資料」，行程沒有今日站點時顯示「今天尚未安排站點」。這些訊息只在對旅程階段有意義時出現；例如旅行已結束後不再提醒加入航班。

## 4. 狀態優先序與主行動規則

### 4.1 主狀態演算法（閱讀規則，不是自動化行為）

系統必須一次只選擇一個主狀態。優先序如下，避免同時出現多個 competing CTA。

| 優先序 | 條件 | 主標題 | 主要 CTA | 副資訊 |
|---:|---|---|---|---|
| 1 | 旅行中，現在位於任一有開始／結束時間的活動之內。 | `正在進行：{活動名稱}` | `查看今日路線` | 顯示結束時間或下一站。 |
| 2 | 旅行中，今天存在尚未開始的時間型活動。 | `下一站：{活動名稱}` | `打開今天路線` | `HH:MM 開始`、地點、距現在多久。 |
| 3 | 旅行中，今天只有無時間活動或沒有活動。 | `今天尚未安排下一站` | `規劃今天路線` | 顯示「可從地圖、候選地點或手動新增開始」。 |
| 4 | 計劃中，距出發 0–7 天。 | `距出發 {N} 天` | `查看出發前清單` | 最多顯示 2 項未確認資料或待處理訊號。 |
| 5 | 計劃中，距出發超過 7 天。 | `旅程正在準備中` | `規劃下一天` | 顯示最早尚未安排的日子。 |
| 6 | 旅程已結束。 | `這趟旅程已完成` | `查看旅程摘要` | 可顯示尚未同步支出或未完成結算；P0 不進行結算。 |

如果兩個活動開始時間相同，按既有 itinerary 排序決定顯示順序。若時間欄位缺失，該活動保留在今日路線中，但不會被選作「即將開始」的主事件。

### 4.2 健康訊號規則

主狀態下方最多顯示三項訊號。使用者可按訊號進入相關位置，惟訊號本身不提供刪除、修復或自動更改操作。

| 類別 | 條件 | 標籤與語句 | 點擊目的地 | 視覺層級 |
|---|---|---|---|---|
| 同步 | 離線或 `pendingCount > 0`。 | `離線已保存 {N} 筆支出` 或 `等待同步 {N} 筆`。 | 支出頁；若在線且待同步，顯示「立即同步」次要行動。 | 墨黑圖示＋暖白底；離線非錯誤。 |
| 航班 | `Flight_Status = pending` 或 `cancelled`。 | `航班待確認`／`航班已取消`。 | 概覽中的航班資料。 | 待確認使用 Journey Yellow 邊框；取消保留紅色文字和文字說明。 |
| 住宿 | 今天是 check-in／check-out 日，或已接近出發而沒有住宿資料。 | `今天 16:00 入住`／`尚未加入住宿資料`。 | 概覽中的住宿資料。 | 中性暖白卡；需今天處理才使用 Journey Yellow 點。 |
| 行程負荷（P0 Lite） | 今日有 7 項以上活動，或相鄰兩項都有時間但間隔小於 15 分鐘。 | `今天安排較緊`。 | 今日路線。 | Journey Yellow 標記，不使用紅色或分數。 |
| 預訂 | 今天相關預訂缺少備註／確認碼，且該資料模型已存在可檢查欄位。 | `今日預訂資料待補`。 | 概覽中的預訂資料。 | Journey Yellow 標記。 |

當訊號多於三項時，先顯示：取消航班、同步、今日入住／退房、行程過緊、待確認航班、缺資料。其餘以 `另有 N 項待處理` 合併成一項可點擊列表；P0 不使用無限捲動的通知中心。

## 5. 視覺規格：Trip Portal 系統下的健康卡

### 5.1 色彩與材料

本功能遵守現有 Tabitime-inspired Trip Portal 的材料系統。健康卡不可回復 Route Blue，亦不可將每個訊號用不同高飽和顏色處理。

| 元素 | 指定樣式 | 用途 |
|---|---|---|
| 主卡表面 | Ink Black `#111111`，可使用低對比 route grid。 | 主狀態與唯一主 CTA。 |
| 主要 CTA | Journey Yellow `#FFC91A`，黑字。 | 只提供一個主行動。 |
| 次要健康列 | Canvas Ivory `#F5F2E8`、Paper White `#FFFFFF`、邊框 `#E3DDCF`。 | 顯示可點擊但非緊急訊號。 |
| 已選取／正在進行 | Ink Black 表面＋Journey Yellow 文字／小點。 | 不使用藍底或藍字白底。 |
| 需要注意 | Journey Yellow 小點、文字或邊框。 | 不以顏色單獨表達；文字必須說明原因。 |
| 破壞性例外 | 只用於航班已取消／同步永久失敗等明確錯誤。 | 保留深紅文字；不把一般離線狀態當錯誤。 |

### 5.2 首頁緊湊卡（Trip Portal）

首頁的今日作戰卡放在 Trip Pass 之後、六張工具卡之前。它採用 Paper White 表面、暖白內框、圓角 `24px`，不與黑色 Trip Pass 競爭。

```text
┌──────────────────────────────────────────────┐
│ 08 / TODAY’S OPERATIONS          2 項待處理   │
│                                               │
│ 下一站：清水寺                                │
│ 10:30 開始 · 12 分鐘後 · 京都東山              │
│                                               │
│ [ 📍 打開今天路線 ]       ● 今天安排較緊       │
│                                  離線已保存 2 筆│
└──────────────────────────────────────────────┘
```

首頁緊湊卡高度上限為 196px（手機）與 176px（桌面）。訊號在手機上橫向可捲動但不隱藏主要文字；每一項最少 44px 高，提供可見的下一項露出量作為捲動暗示。

### 5.3 行程概覽完整卡

概覽頁的完整卡保留主狀態、時間脈絡和最多三項健康訊號。它取代現有 `JOURNEY / NOW` 卡，而非追加另一張大型卡片。

```text
┌──────────────────────────────────────────────┐
│ JOURNEY / HEALTH                  第 4 / 8 天 │
│                                               │
│ 下一站：伏見稻荷大社                          │
│ 13:20 開始 · 27 分鐘後 · 需要轉乘 1 次          │
│                                               │
│ [ 📍 打開今天路線 ]                            │
├──────────────────────────────────────────────┤
│ ● 今天安排較緊      › 今日路線                 │
│ ◌  航班待確認       › 航班資料                 │
│ ◌  已連線，所有離線支出已同步                  │
└──────────────────────────────────────────────┘
```

完整卡的健康列採 `button` 或帶有鍵盤語義的連結，每一列有不超過一行的標題和一行次要說明。無可點擊目的地的「全部已同步」訊號為靜態 `role=status`，不可偽裝成按鈕。

## 6. 桌面與手機行為

| 面向 | 桌面（≥1024px） | 手機（<768px） |
|---|---|---|
| 主卡內部 | 主狀態文字在左、CTA 與旅程天數在右；訊號列可在下方兩欄排列。 | 先顯示旅程天數、主狀態、時間／地點，再是全寬 CTA；訊號單欄排列。 |
| 主 CTA | 最少 44px 高，內容寬度；不延伸至整卡。 | 全寬，最少 48px 高，置於訊號之前，拇指容易到達。 |
| 健康訊號 | 最多三列，兩欄或三欄視可讀性調整。 | 單列 48–56px，高訊號多時以左右捲動列表顯示，但保留「另有 N 項」。 |
| 工具卡關係 | 健康卡不搶走六張工具卡；CTA 只深連結到現有工具頁。 | 健康卡位於 Trip Pass 之後、工具卡之前；不增加底部導航項目。 |
| 安裝提示 | 首頁可顯示，但不得遮蔽健康卡 CTA。 | 行程工作區保持既有隱藏規則，避免覆蓋行程與健康資訊。 |

## 7. 互動與微文案規則

### 7.1 主要互動

主 CTA 一律深連結到既有頁面，不開啟新的 modal。`打開今天路線` 會進入 itinerary，選中目前日期，並在有下一站時將該卡聚焦／高亮。`規劃今天路線` 進入同一頁並開啟既有新增站點入口，但**不自動儲存**任何內容。`查看出發前清單` 在 P0 只進入概覽並聚焦到缺資料區；真正的出發前包是 P2。

### 7.2 訊號行為

健康訊號只提供解釋與深連結。例如「航班待確認」開啟概覽航班區，不直接切換航班狀態；「今天安排較緊」開啟今日路線，不自動移動項目。若離線支出待同步，健康卡顯示 `立即同步` 作為次要 button，重用現有同步函式，成功與失敗均使用已有 toast 及持續狀態卡回饋。

### 7.3 文案準則

文案必須指向具體資料與下一步。應寫「13:20 開始 · 27 分鐘後」，不寫「你有新提醒」；應寫「航班待確認」，不寫「注意」；應寫「已在此裝置保存 2 筆支出」，不寫「同步有問題」。不使用誇張、焦慮或命令式語氣。

## 8. 無障礙、動效與失敗情境

| 項目 | 要求 |
|---|---|
| 顏色 | 所有狀態均要有文字；Journey Yellow、墨黑、紅色不可成為唯一訊號。 |
| 鍵盤 | CTA、訊號列與次要同步按鈕應按視覺順序 Tab；focus 使用 Ink Black outline 和 Canvas Ivory halo。 |
| 螢幕閱讀器 | 主狀態容器使用語義化 `section` 與可見標題；同步狀態使用 `role=status`、`aria-live=polite`，不用對每次倒數刷新廣播。 |
| 時間 | 以使用者裝置的本地時間顯示；若無時區資料，避免聲稱特定時區。倒數每分鐘更新，低於一分鐘時顯示「即將開始」而不是每秒動畫。 |
| 動效 | 卡片進場只用 160–220ms 的 opacity／translate；不對健康警示閃爍，不使用循環脈衝吸引注意。遵從 `prefers-reduced-motion`。 |
| 載入 | 顯示高度固定的 skeleton，不讓 Portal 工具卡在資料回來後跳動。若其中一個資料源失敗，仍顯示其餘資料並以「暫時無法讀取預訂資料」說明。 |
| 離線 | 離線視為可工作狀態。只有新增內容無法被本機保存或同步永久失敗時才使用錯誤視覺。 |

## 9. P0 明確不包含的內容

本規格不包含推播通知、背景定位、航班實時 API、Gmail OAuth、完整確認信解析、日程自動重排、付款、結算演算法、角色變更或新資料庫表。這些能力雖有價值，但會擴大權限、可靠性、免費層成本和驗證範圍；應在旅程健康卡被證明有使用價值後才逐項設計。

## 10. 實作前需要確認的決策

請在實作前確認下列決策；未確認前不應改動程式。

| 決策 | 建議預設 | 需要確認的原因 |
|---|---|---|
| A. 首頁位置 | Trip Pass 後、六張工具卡前。 | 決定首頁首屏的資訊密度。 |
| B. P0 Lite 行程過緊門檻 | 每日 ≥7 項活動，或相鄰時間型活動少於 15 分鐘。 | 需避免過多無價值警示。 |
| C. 旅行前健康期 | 只在出發前 7 天顯示「出發前」健康訊號。 | 避免太早讓尚在靈感階段的使用者感到被催促。 |
| D. 同步行動 | 健康卡有待同步時顯示「立即同步」次要行動。 | 重用現有離線同步，但會增加一個入口。 |
| E. 今日無行程 | 顯示「今天尚未安排下一站」與「規劃今天路線」。 | 將空白日視為中性規劃機會而非錯誤。 |
| F. 已完成旅程 | 顯示旅程摘要 CTA，並保留未同步支出提醒。 | 影響舊行程的回顧體驗。 |

## 參考資料

[1] [TripIt — Travel Itinerary App](https://www.tripit.com/web)  
[2] [Wanderlog — Travel Planner](https://wanderlog.com/)  
[3] [Trip-WebApp Market Improvement Report](./trip-webapp-market-improvement-report.md)  
[4] [Trip-WebApp UX Redesign Audit](./ux-redesign-audit.md)
